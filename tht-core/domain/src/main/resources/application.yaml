spring:
  application:
    name: domain
  config:
    import: security/application-security.yaml
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true