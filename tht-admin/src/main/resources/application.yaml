spring:
    config:
        import: security/application-security.yaml
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                format_sql: true
        show-sql: true

logging:
    level:
        p6spy: info
        org:
            hibernate:
                SQL: DEBUG
                orm.jdbc.bind: TRACE
